# Environment Configuration
NODE_ENV=development

# API Ports
ADMIN_API_PORT=3000
PARTNER_API_PORT=3001
CUSTOMER_API_PORT=3002

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=tulealtapp
DB_PASSWORD=tulealtapp
DB_NAME=tulealtapp
DB_ROOT_PASSWORD=rootpassword

# JWT Configuration
# ⚠️ IMPORTANTE: Cambiar este secret en producción por uno seguro y único
JWT_SECRET=default-secret-change-in-production
JWT_EXPIRES_IN=1h

# s3 config
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=minioadmin
S3_BUCKET_NAME=tulealtapp-images
S3_REGION=us-east-1
S3_FORCE_PATH_STYLE=true

# Other Configuration
# LOG_LEVEL=info

# Email (SMTP)
# Configuración para Desarrollo (GreenMail - servidor SMTP local con SSL)
# Para usar GreenMail en desarrollo, usar estos valores:
# SMTP_HOST=localhost (o greenmail si usas Docker)
# SMTP_PORT=3465 (puerto SSL de GreenMail)
# SMTP_SECURE=true
# SMTP_USER= (opcional, GreenMail acepta cualquier email)
# SMTP_PASSWORD= (opcional)
# SMTP_FROM=noreply@tulealtapp.local

# Configuración para Producción (Hostinger)
# SMTP_HOST=smtp.hostinger.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=tu_usuario_hostinger
# SMTP_PASSWORD=tu_password_hostinger
# SMTP_FROM=noreply@tudominio.com

# Valores por defecto (desarrollo con GreenMail)
SMTP_HOST=localhost
SMTP_PORT=3465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@tulealtapp.local

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

